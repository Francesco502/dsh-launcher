# Changelog

本文件记录 `dsh-launcher` 的用户可见变更，格式遵循 Keep a Changelog，版本号遵循语义化版本规范。

## [Unreleased]

后续变更写在这里，发布时再移动到对应版本。

## [0.3.1] - 2026-09-03

### Changed

- 启动器启动的 DSH 支持从浏览器地址栏或书签直接访问 localhost:3080 与 127.0.0.1:3080；本机主动导航自动完成认证交换，保留 DSH 的 Cookie、API 和跨站隔离检查。

- 首页收缩为一个状态化主按钮、“打开 Web UI”和“更新 DSH”，托盘固定为四个菜单项；删除“更多工具”页和 50ms 悬停动画。
- 普通启动仅本地发现 `data\npm-global` 和系统 npm 全局 DSH；窗口隐藏后停止周期健康检查，健康检查不再调用 `netstat.exe` 或重复发现 Node/npm。
- Node.js 缺失时只打开官方下载页；启动器不再下载、校验、解压或回滚 Node 运行时。
- DSH 安装与更新统一为轻量同盘事务，按精确版本暂存并校验后交换完整管理 npm 前缀；提交后固定保留最新版本，不再因启动失败恢复旧版。
- 启动器更新降级为版本检查和 Release 页面跳转，不再下载或自替换 EXE。
- CLI 立即收缩为 `start`、`stop`、`upgrade`、`open` 四个动作。

### Removed

- 删除旧数据迁移、回收站清理、完整诊断页、便携环境修复、启动器自替换助手、npm audit、依赖锁生成、生命周期脚本白名单、3081 候选服务和 Quota 预检。
- 删除旧 `--data-dir`、重启、修复、迁移、打开数据目录和启动器更新入口；旧 `data\runtime` 与迁移标记仅忽略，不自动删除。

### Fixed

- 修复键盘聚焦按钮后 Enter 不触发操作，以及操作期间禁用按钮后丢失焦点的问题；四个按钮支持 Enter，操作完成后仍可继续使用 Space 和 Tab。
- 兼容 DSH 认证地址的 Cookie 与首页重定向流程，最终页面通过健康检查后才报告启动成功，修复真实服务已启动却持续等待直到超时的问题。
- 修复认证地址因日志超过 8 KiB 或中文字符截断而丢失；增量读取并重新验证地址，认证不可用时显示恢复提示。
- 更新恢复重新校验包、入口和运行依赖；候选缺失或损坏时保留配置模式并进入重装流程，不再错误提交空目录或未完成的下载。
- 修复 `dshctl.cmd` 在非 UTF-8 默认环境下的中文乱码，直接 EXE 的控制台输出使用 Unicode；CI 和正式发布增加 CLI 编码与退出码检查。
- 启动 DSH 时隔离调用方的输入输出管道，避免 CLI 已退出后，脚本仍因后台 DSH 持有管道而持续等待。
- Node.js 安装提示明确要求从托盘退出后重新运行启动器，避免只重开面板而继续使用旧安装路径。
- DSH 更新继续比较官方全部 SemVer，可识别 `0.1.2-alpha.4` 等未进入 `latest` 标签的预发布版本，并严格禁止降级。
- npm 候选提升改为交换完整前缀，避免只移动 DSH 包目录导致同级依赖丢失。
- 健康检查要求首页同时返回 DSH 专用 `__DSH_BOOT__` 标记，普通占用 3080 端口的 HTTP 服务不再触发蓝鲸、运行状态或 Web UI。
- 兼容 DSH `0.1.2-alpha.4` 的 Web 认证 URL：健康检查识别官方 401 挑战，并重新验证 DSH 输出的本地认证地址；“打开 Web UI”不在界面显示令牌。
- 系统 DSH 转入启动器管理副本后，后续更新、同版本重装和中断事务恢复继续使用 Windows 用户配置。
- 最新版启动失败后允许重新安装完全相同的版本；更新事务和损坏事务恢复只提交最新暂存，不再恢复旧 DSH。
- 更新事务与配置状态改为同盘原子写入，补齐候选运行依赖、可用磁盘空间、npm 缓存和孤立暂存目录检查。
- 下载阶段可取消，停止、目录提交和启动验证阶段明确不可取消；操作结果不再被即时健康检查覆盖。
- 顶层窗口按当前 DPI 创建，页脚改为支持 Enter/Space 和可见焦点的文本按钮，并修复 Web UI、DSH 更新按钮的真实启用条件。
- DSH、npm 和启动器更新检查的临时文件定向到便携 `data\tmp`，启动器版本查询增加双层超时。
- `dshctl.cmd` 改为阻塞等待、输出 UTF-8 结果并保留退出码；EXE 增加“DSH启动器”产品名、文件版本和原始文件名。
- 强制停止改为结束整个 DSH 进程树，避免只结束父进程后残留 3080 子进程。
- 修复状态文本透明重绘造成的更新阶段文字重影；亮色与高对比度切换时同步重建控件背景画刷。
- 首次关闭窗口改为非阻塞系统通知，窗口立即隐藏到托盘，不再弹出可能跨显示器错位的确认框。

## [0.3.0] - 2026-09-01

### Added

- 增加本地 DSH 发现：依次检查启动器便携数据、Windows `PATH`、npm 全局前缀和常见用户级 npm 目录。
- 诊断详情显示当前实际使用的 DSH 来源、版本、入口文件和 Node.js 路径。

### Changed

- 普通启动和服务启动只检查本机文件，不联网查询 Node.js、DSH 或最新版本。
- 已安装的有效旧版 DSH 可以直接启动；只有用户明确点击“更新 DSH”时才联网比较完整 SemVer。
- “验证并修复”更名为“安装/修复便携环境”，明确它是可选的联网安装能力，不是每次启动的前置条件。
- 修复便携环境时保持本机现有 DSH 的精确版本；只有完全未安装 DSH 时才使用清单引导版，版本升级仍由“更新 DSH”独立触发。
- 系统 DSH 保持 Windows 用户原有环境和配置；便携 DSH 继续把配置、缓存、日志和状态限制在 EXE 同目录的 `data`。

### Fixed

- 修复本机已有官方 DSH 仍被要求下载启动器自管 Node.js 和 DSH 的冗余行为。
- 修复便携 DSH 缺少自管 Node.js 时无法复用本机 Node.js 的问题。

## [0.2.1] - 2026-09-01

### Changed

- 应用名称统一为“DSH启动器”，移除首页“常用操作，一眼完成”副标题并压缩无效留白。
- DSH 更新检查改为比较官方 npm 包发布的全部版本，不再只读取可能滞后的 `latest` 标签。
- 新安装运行时的 DSH 引导版本更新为官方 `0.1.2-alpha.3`，并适配其当前依赖声明。

### Fixed

- 修复官方已发布 `0.1.2-alpha.3` 时仍把 `0.1.1-rc.2` 识别为最新版本的问题。
- 增加稳定的 Windows AppUserModelID 和小图标回退，修复任务栏图标可能显示为空白文档的问题。

## [0.2.0] - 2026-09-01

### Changed

- 将应用和托盘图标更换为 DeepSeek 官方鲸鱼标志。
- DSH 正常运行时使用官方蓝色鲸鱼；服务未使用、不可用或不健康时使用黑色鲸鱼。
- 将应用版本更新为 0.2.0，并把运行时、npm 包/缓存、配置、日志、状态和更新暂存严格统一到 EXE 同目录的 `data`。
- 主便携包改为轻量结构，仅包含启动器、`portable.flag`、运行时清单和 `dshctl.cmd`；首次使用时按需下载 Node.js/DSH，不再发布离线运行时包，也不再捆绑 PowerShell 7。
- 启动器侧改用 Windows 10/11 自带 `powershell.exe` 完成下载和解包，删除约 245 MiB 的重复 PowerShell 运行时。
- 增加完整运行时清单、版本/哈希校验、严格便携目录约束和系统盘存储警告。
- 单实例互斥体改为按规范化 EXE 路径隔离；同一便携目录仍保持单实例，不同便携目录可并行运行。
- 精简主窗口为“常用操作”和“更多工具”两层：首页只保留启动/停止、Web UI 与 DSH 更新，维护、诊断和启动器更新移入二级页，并移除会被裁切的长数据路径。

### Added

- 保留 `--data-dir` 兼容解析，但拒绝任何不是 EXE 同目录 `data` 的路径；取消环境变量和其他磁盘回退。
- 增加旧版 C 盘数据的逐文件 SHA-256 迁移事务、失败回滚和 DSH 成功运行后的二次确认回收站清理。
- 主窗口增加“打开 Web UI”“打开数据目录”“验证并修复”“诊断详情”“取消”，托盘在 Explorer 重启后自动恢复。
- DSH 候选更新改为完整 SemVer 严格不降级、锁依赖安装、生命周期脚本白名单、高危 npm 审计和 3081 端口 JSON 预检；失败离线回滚。
- 启动器更新改为 `data\updates` 下的事务助手、父进程/路径/哈希校验和健康握手，成功前保留备份。

### Fixed

- 修复服务进程存在但 Web UI 暂时无响应时被误判为未运行的问题。
- 修复状态消息逆序显示、停止状态错误显示为绿色，以及更新失败可能留下半替换启动器的问题。
- 增加优雅退出超时后的受控强制终止记录、日志轮转、DPI/深色/高对比度适配和 Esc 取消快捷键。
- 统一使用适配简体中文的 Microsoft YaHei UI 字体，并补齐 Enter 键按钮激活与后台诊断收集，避免诊断详情阻塞主窗口。
- 允许完整但低于 bootstrap 的旧版 DSH 进入原子更新，并短暂保留操作结果，避免被后台健康轮询立即覆盖。

## [0.1.2] - 2026-08-26

### Fixed

- 修复从任务栏托盘菜单检查启动器更新时，将“已是最新”误判为更新完成并退出程序的问题。

## [0.1.1] - 2026-08-26

### Added

- 增加启动器自身更新能力，从官方 GitHub Release 下载并校验 EXE。
- 增加版本管理标准、发布门禁和机器可读的 Release manifest。

### Changed

- DSH 正常运行时托盘图标保持彩色；DSH 不可用或不健康时切换为灰度图标。

### Fixed

- 允许关闭由启动器外部方式启动、但确实监听本地 `3080` 端口并执行 DSH `web` 命令的进程。
- 对 `3080` 上的无关进程保持拒绝关闭，避免按端口误杀其他服务。

## [0.1.0] - 2026-08-25

### Added

- 发布首个公开的 Windows 原生 DSH 启动器。
- 支持安装/更新、启动、重启、关闭 DSH 和打开 Web UI。
- 支持托盘驻留、单实例窗口和命令行操作入口。
