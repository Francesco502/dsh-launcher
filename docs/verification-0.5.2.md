# 0.5.2 候选验收记录

日期：2026-09-14。本次交付本地候选，未创建标签、推送或发布 Release。候选清单使用 `local-working-tree`，不作为正式发布身份。

## 自动检查

- GNU / MSVC：各 63 项 Rust 业务测试、6 项图标测试通过；常规运行的 2 项显式忽略保持原有规则。fmt、双工具链 Clippy、Release 构建、CLI 中文与退出码检查通过。
- 两种工具链各完成 100 次 Slint 窗口生命周期检查，验证不透明绘制、组件释放和资源稳定。定时器至截图耗时不作为真实按钮响应延迟。
- JavaScript 29 项通过，包括真实 npm 本地源、所选依赖升级、未选依赖及本地来源保留、断网和配置并发变化。
- 新增 Windows 文件占用测试：已完成事务清理失败时保留新安装，普通恢复入口可用，新更新被阻止；解除占用后清理成功。无效事务不会因为 DSH 修复标记而被忽略。
- 新增结果测试覆盖普通取消、提交前失败、成功回滚与恢复失败，避免错误显示已恢复。
- 下载校验 7 项通过：有效包、断网、错误哈希、错误版本、错误架构、ZIP 路径越界和重复条目。
- 六个本地资产、四文件便携 ZIP、版本资源及 SBOM 通过发布校验。Git 连通性检查通过。

## 独立业务验证

使用 `test-isolated-port` 的 3081 构建和独立复制的便携目录，未停止或替换日常 3080 服务。业务构建与默认发布构建分别保留身份，不混用。

- 第三方插件源设为不可达地址，保留启用的 quota 0.1.0。真实 DSH 从 `0.1.3-alpha.2` 升级到 `0.1.5-rc.2` 并恢复运行；28 个插件文件、依赖/锁文件和选择设置的 SHA-256 保持不变。事务仅交换 DSH 前缀及安装状态，没有生成插件更新脚本。
- 再次检查当前版本立即返回无需更新，原服务 PID 保持不变。
- 强制同版本修复并使用仅测试构建内的启动失败钩子，候选进程实际退出；原入口文件哈希恢复、服务恢复运行、事务记录清理，返回“失败后已恢复原版本和原运行状态”。
- 停止服务后注入无效 profile JSON，候选在本地预检阶段被拒绝，原入口哈希不变，返回“提交前失败，原安装未变”。
- 恢复配置后成功重新安装同版本 `0.1.5-rc.2`，原本停止的服务保持停止，修复标记清理；上述 28 个插件文件及设置的哈希仍全部不变。
- 显式隔离插件集成测试通过：首次确认取消后依赖清单不变、事务清理；重试将 quota `0.1.0 → 0.2.2`，DSH 包元数据和保存的插件选择不变，3081 保持停止。确认信息不再显示无变化的 DSH 版本迁移。

`joint_update::tests::isolated_plugin_update_fixture` 是显式忽略的真实 npm 集成检查，仅在 `test-isolated-port` 构建中存在。将测试可执行文件放入独立便携夹具根目录，并设置 `DSH_PLUGIN_UPDATE_FIXTURE_ROOT` 为该目录；要求 3081 停止、存在可升级的已启用 npm 插件。测试取消第一次确认后重试，核对 DSH 包元数据及保存的插件选择不变，服务保持停止。默认发布构建没有新增 CLI 参数。

## 实际界面及限制

- 默认 GNU 候选的关于页面已实际观察：两个次要按钮等宽等高、边框与背景一致；Tab 可定位检查更新，Enter 可触发并完成检查，焦点返回版本入口，Space 可再次打开关于。
- 点击 GitHub 后观察到浏览器标题为本项目；随后 Computer Use 因无法可靠确定浏览器 URL 终止本轮界面自动化，因此不将 URL 核对及后续插件页实际交互标为通过。
- 当前 Windows 会话的截图、测试日志和本地资产保存在 `.tmp-release-052/`。未补做已取消的其他 DPI/本地显示项目，也未声称按钮 P95 延迟通过。
- 未进行远端 CI、正式资产下载复验或日常安装替换。

## 审查优化追加验收

同日完成错误摘要、托盘状态及放弃勾选文案优化，版本保持 0.5.2。此前业务更新与回滚记录仍作为基线；当前候选以 `.tmp-release-052/ui-polish/` 的追加记录和身份清单为准。

- 更新失败的第一行现在明确安装和恢复结果，摘要保留结果及首行故障原因；完整诊断仍可展开。提交前清理失败不会误报“更新已提交”。相关结果排序、摘要截断和恢复失败分支回归通过。
- 托盘和主窗口共用动作名称、启用条件及 Web 可用状态。回归覆盖进程运行但认证不可用、安装/修复入口、初始化、弹窗、可取消和不可取消任务。弹窗打开、回答及窗口关闭时立即同步菜单状态；健康图标与实际运行状态分别处理。托盘原生右键菜单的实机异常态交互未在本轮另行截图验证。
- 使用新增独立 3081 副本实测：返回时显示“继续编辑 / 放弃修改”，继续编辑保留临时勾选；关闭时明确说明会关闭窗口，放弃后窗口关闭，重新打开仍读取原已保存勾选。
- 在该副本中制造 npm 源与锁文件不匹配，实际错误窗口默认直接显示“提交前失败，原安装未变”及失败原因。恢复测试源后，插件设置、依赖清单、锁文件和插件包元数据 SHA-256 全部不变，无残留更新事务。日常 DSH 未操作。
- GNU / MSVC 各 65 项业务测试和 6 项图标测试通过；双工具链 Clippy、Release 构建、各 100 次窗口生命周期检查及 CLI 检查通过。GNU 一次并行测试中的既有认证截止时间用例失败；保留失败日志，单项复核与完整套件重跑通过，未修改时间阈值。
- 已刷新并校验六个本地资产及便携 ZIP；前一版资产保留在 `ui-polish/baseline-assets/`。本次未发布。
