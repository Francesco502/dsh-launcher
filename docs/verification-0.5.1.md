# 0.5.1 验收记录

日期：2026-09-10。本文件记录发布前验收；正式提交身份、六资产哈希与远端构建以同名 GitHub Release 的 manifest、SBOM 和 Actions 记录为准。

## 实现与环境

- Slint 1.17.1 / winit 软件渲染；居中版本号进入关于，关于包含 GitHub 项目和启动器检查更新。公开 README / Release 页面提供 Slint 官方徽章与许可链接。
- 插件更新使用已保存的启用状态，查询原 npm 源全部 SemVer（含预发布），精确版本确认；未选直接依赖保留锁定内容，非 npm 来源保持原样并列出。复制型 Git / file 依赖保留实体，外部目录链接保留原目标。
- 联合更新以同一记录保护 DSH 前缀、安装模式、插件 node_modules、package.json 和锁文件；提交前预检，失败回滚，保留恢复记录直至完成。
- 日常 DSH 占用 3080，未停止或替换日常安装。真实业务验收使用 `test-isolated-port` 构建的 3081；正式默认构建仍为 3080。隔离构建不等同于公开下载 EXE。
- 沿用远程 100% DPI；本地及 150% DPI 已取消。真实按钮反馈 P95 ≤100 ms 未验证，不标为通过。定时器 / 截图耗时不是按钮响应延迟。

## 自动检查

- GNU / MSVC：Rust 61 通过、2 显式忽略；图标 6 通过。fmt、双工具链 Clippy 和 Release 构建通过。
- Slint 外部 60 秒超时回归：两种工具链各 100 次生命周期，背景不透明、内容绘制、组件释放与资源平台检查通过。
- JavaScript 29 通过，无失败或跳过。npm 原生解析器覆盖预发布排序、不降级、选择去重、别名、本地来源和配置指纹。
- 真实 npm 本地源集成覆盖所选包升级、未选直接依赖锁定不变、外部链接及复制型 Git 来源保留、配置并发变化、来源变化和断网失败。发现 npm 会回退旧缓存后，版本查询改用独立临时缓存，断网测试通过。
- Rust 事务测试覆盖每个目录移动边界中断、重复回滚、首次安装回滚、后续文件被 Windows 锁定时还原前项，以及清理目录联接不删除外部目标。
- 默认构建 CLI 中文输出、公开命令和退出码通过；四文件便携包、六资产元数据和 SBOM 校验通过。
- 下载校验 7 项通过：有效包、断网、错误哈希、错误版本、错误架构、ZIP 越界路径与重复条目。
- 隔离官方 0.5.0 → 候选 0.5.1 自更新四项通过：成功、候选就绪失败回滚、文件占用回滚、中断恢复；验证四文件组、窗口重新出现及用户数据保留。首次与插件更新并行测试被操作互斥锁拒绝，串行重试通过。

## 真实 UI 与业务证据

- 默认构建首次显示和关于界面已观察：背景不透明，四个操作按钮同尺寸对齐，底部版本号居中，关于不含 Slint 内容。没有对日常服务执行操作。
- 隔离构建验证 GitHub 默认浏览器打开、关于内检查更新、未保存勾选提示、取消及焦点恢复；更新确认和多行结果均为窗口内不透明可滚动层。
- 使用公开 `@francescoli/dsh-quota@0.1.0` 的标准 scoped 安装，发现包内入口引用 `dsh-quota` 不匹配。候选 0.2.2 预检同样拒绝，原版本仍为 0.1.0，未假装修复第三方包。
- 另建合法 npm 别名 profile：`dsh-quota: npm:@francescoli/dsh-quota@0.1.0`，使用同一真实公开包，无源码改写。
- 运行中联合升级 DSH `0.1.3-alpha.2 → 0.1.5-alpha.2`、quota `0.1.0 → 0.2.2` 成功，3081 服务恢复健康，原插件选择保留，事务记录清理完成。
- DSH 已为 `0.1.5-alpha.2` 时仍检查插件：取消保留 quota 0.1.0；重新确认升级至 0.2.2，原本停止的 DSH 保持停止。
- 插件页单独更新：quota `0.1.0 → 0.2.2`，DSH 版本不变，原本运行的 3081 服务恢复运行，页面刷新显示 0.2.2。
- 截图、完整测试日志、隔离构建身份及事务证据保存在本地 `.tmp-release-051/`；不公开用户配置、认证日志或私有路径内容。

## 证据边界

- 真实启动失败回滚：仅在 3081 测试构建内，联合候选完成交换后将其启动入口替换为退出码 93 的故障夹具。候选进程实际退出，系统恢复 DSH 0.1.3-alpha.2、quota 0.1.0 及 3081 运行状态，事务与故障标记清理；日常 3080 进程身份不变。该一次性注入代码不编入默认发布构建。
- 长列表使用 20 项明确缺失的布局夹具：滚动至第 20 项后，更新、修复、保存和取消操作区保持可见；取消正常返回首页。该夹具仅证明长列表布局，不冒充真实依赖升级。
- 六个公开资产及正式下载包复验在 Release 完成后执行；不得用本地候选哈希代替公开资产身份。
